#pragma once
#include <string>

static struct SceneType
{
    std::string kTitle  = "Title";
    std::string kStage  = "Stage";
    std::string kResult = "Result";
    std::string kLoad   = "Load";
    std::string kShared = "Shared";
}SceneType;